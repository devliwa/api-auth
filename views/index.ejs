<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Express App</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #f0f0f0, #e6e9f0);
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    header {
      width: 100%;
      padding: 20px;
      text-align: center;
      background: linear-gradient(135deg, #3498db, #2ecc71);
      color: white;
      font-size: 28px;
      font-weight: bold;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
    }

    main {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 40px 20px;
    }

    .button-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
      gap: 20px;
      width: 100%;
      max-width: 700px;
      margin-bottom: 40px;
    }

    form {
      display: flex;
      justify-content: center;
    }

    button {
      width: 100%;
      padding: 15px 30px;
      font-size: 16px;
      font-weight: bold;
      border: none;
      border-radius: 12px;
      color: white;
      cursor: pointer;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    button:hover {
      transform: translateY(-4px);
      box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.2);
    }

    #noAuth {
      background: linear-gradient(135deg, #3498db, #2980b9);
    }

    #basicAuth {
      background: linear-gradient(135deg, #2ecc71, #27ae60);
    }

    #apiKey {
      background: linear-gradient(135deg, #f1c40f, #f39c12);
      color: #333;
    }

    #bearerToken {
      background: linear-gradient(135deg, #e74c3c, #c0392b);
    }

    .response-area {
  width: 100%;
  max-width: 700px;
  background: #fff;
  border-radius: 12px;
  box-shadow: 0px 6px 16px rgba(0, 0, 0, 0.15);
  padding: 25px 30px;
  text-align: left;
  border-top: 5px solid #3498db; /* Highlight bar */
  animation: fadeIn 0.6s ease-in-out;
}

.response-area h2 {
  margin-top: 0;
  font-size: 20px;
  color: #333;
  margin-bottom: 15px;
  border-bottom: 1px solid #eee;
  padding-bottom: 10px;
}

.response-message {
  background: #f9f9f9;
  border-left: 5px solid #3498db;
  padding: 15px 20px;
  border-radius: 8px;
  font-size: 17px;
  line-height: 1.6;
  color: #444;
}

.response-message p {
  margin: 0;
}

/* Success, Error, Warning Variants */
.response-message.success {
  border-left-color: #2ecc71;
  background: #eafaf1;
  color: #2c662d;
}

.response-message.error {
  border-left-color: #e74c3c;
  background: #fdecea;
  color: #a12622;
}

.response-message.warning {
  border-left-color: #f1c40f;
  background: #fff8e1;
  color: #7c6f00;
}

/* Fade-in animation */
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

    footer {
      text-align: center;
      padding: 15px;
      font-size: 14px;
      color: #777;
      background: #f9f9f9;
      border-top: 1px solid #ddd;
    }
  </style>
</head>

<body>
  <header>
    API Authentication 
  </header>

  <main>
    <div class="button-container">
      <form action="/noAuth" method="get">
        <button id="noAuth" type="submit">No Auth</button>
      </form>

      <form action="/basicAuth" method="get">
        <button id="basicAuth" type="submit">Basic Auth</button>
      </form>

      <form action="/apiKey" method="get">
        <button id="apiKey" type="submit">API Key</button>
      </form>

      <form action="/bearerToken" method="get">
        <button id="bearerToken" type="submit">Bearer Token</button>
      </form>
    </div>

   <div class="response-area">
    <h2>Server Response</h2>
     <div class="response-message">
     <p><%= content %></p>
     </div>
   </div>
  </main>

  <footer>
    &copy; <%= new Date().getFullYear() %> Devliwa. All rights reserved.
  </footer>
</body>

</html>